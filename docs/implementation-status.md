# Printemps 验收清单

当前结论：**尚未完成整体验收**。本清单区分实现、局部证据与端到端证据；不能用静态设计、示例波形或单元测试替代真实桌面操作。

| 要求 | 当前证据 | 仍需完成 |
| --- | --- | --- |
| Printemps 品牌、暗色圆润 DAW、中英、1440×900 / 1280×800 | `public/brand`、Pen 导出、真实组件浏览器预览；1280×800 详情栏折叠/展开验证 | Electron 各尺寸与全部状态逐屏检查 |
| 单文件选择/拖入后直接进入工作区 | AudioImporter 的真实 FFmpeg 测试、受限 preload API | macOS 选择器导入已通过；拖放、导入失败/退出全过程待验 |
| 工作区无导入/历史入口，首页进入全部项目 | 当前 renderer；历史独立页面的浏览器检查 | macOS 重启打开、声部搜索、保留删除/恢复已通过；多项目分页与结果/任务状态筛选已在浏览器验证；运行状态、取消状态持久化与重开重试已原生验证 |
| 项目/音轨 inline 改名 | 字段级保存、磁盘并发写入/重新打开测试；浏览器 Esc、失焦保存与重试恢复 | macOS 改名及正常退出重开持久化已通过；长名称浏览器检查通过；未提交中文改名关窗保存已原生通过；IME 合成中关窗待验 |
| 53 声部搜索/分类/组合、按需缓存模型 | 固定版本清单、校验/取消测试；浏览器隐藏选择保留 | 原生下载中断/离线/不足空间恢复 |
| 04/05 弹窗、工作区任务行、完成后试听 | renderer、服务测试、真实 drums→bass 双模型测试 | macOS 下载弹窗与实时任务行、完成后切换分轨已通过；分离期间试听待验 |
| Other 必须显示；二次分离原位插入 [Other, targets]，来源保留并隐藏 | domain/service 测试，真实逐步及二次分离重建误差 <1e-5、原位替换及混音设置继承 | macOS 原位替换、75% 进度取消、四音轨保留及重开重试配置已通过；失败恢复待验 |
| 私有结果、WAV/FLAC 独立导出 | 私有路径限制、真实 FFmpeg 格式/重名/部分失败测试；真实模型结果导出后修改副本不改变私有音频；选目录期间的音轨替换不破坏已开始的导出 | macOS WAV/FLAC 导出已通过，用户确认正常，打开导出副本目录已验证；失败恢复及试听端到端待验 |
| 同步波形、定位、缩放、循环、S/M/增益/原始对比 | AudioEngine 共同时间源、循环及逐步结果无全局暂停的调度测试；组件键盘推子检查 | 真实可听输出、多轨同步、循环及分离期间试听 |
| BPM/调性/拍号/第一拍手动设置、推荐星标、复位 | 域测试、真实 Beat This + WASM 测试；星标预览 | macOS 手动四参数及复位已通过；撤销和真实音乐样本评估待验 |
| 显式分析，进入不自动分析；节拍器 Switch、音轨网格、时间格式 | 源码及时间数学测试 | macOS 首次无自动分析、Switch、6/8 手调、小节格式及修正后刻度已验证；节拍器实际听音待验 |
| 设置设备/语言/缓存与导出目录 | 设置路径/竞争测试；本机 CPU 运行证据 | macOS 中英切换及重启持久化通过；各平台目录选择、CUDA 待验，MPS 未通过本机验证 |
| 分析工具内置、统一应用差分更新 | 包内分析测试；真实 Linux 0.1.0→0.1.1 安装包差分重建与 SHA-512 校验通过 | 原生升级前后缓存保留、真实发布服务与安装重启、签名安装 |
| Windows/macOS/Linux | native build 工作流；macOS arm64 测试包；Linux x64 容器内 AppImage 及包内分析通过 | 78f7ea7 的 NSIS、DMG/ZIP、AppImage 与包内分析全部通过；Windows/Linux GUI、安装/卸载与升级待验 |
| 退出保存、取消子进程、异常中断保护 | shutdown/store 测试，关闭保存握手代码 | macOS 活跃分离启动阶段退出、进程结束及项目重开已通过；未提交改名关窗保存已原生通过；更新重启及失败重试待验 |
| 设计交付与说明 | `design/` PNG、HTML、交互说明；原 Pen 文档 | 核对当前 Pen 可编辑内容与最终实现一致性 |

## 当前交付版本与证据

- 应用版本：0.1.1；macOS 测试包 `release/mac-arm64/Printemps.app`，由 2027108 构建，含完整结果恢复及节拍器首拍调度修复。生产构建、深度严格签名完整性与包内真实分析均通过。日志 `/tmp/printemps-final-package.log`。ad-hoc 签名，未发布、公证。
- 最新已完成三平台安装包 CI：[35485443673](https://github.com/Means88/printemps/actions/runs/35485443673)，提交 4e370f9，Windows NSIS、macOS DMG/ZIP、Linux AppImage 与包内分析全部成功。包含隐藏来源音轨、音轨管理及节拍器调度调整；其后的弹窗和音乐工具栏样式修正以本地构建及浏览器检查为证据。
- 当前普通测试 63 项通过，2 项真实运行时测试默认跳过。显式内置分析测试已通过。真实 drums→bass 逐步分离、二次分离、结果对齐及导出副本隔离有独立运行时证据；合成输入不是歌曲质量或性能基准。
- 原生 macOS 已验证单文件导入、改名/关闭保存/重开、显式分析、四项音乐参数和推荐复位、模型下载、首轮与二次分离、启动阶段取消/退出、WAV/FLAC 导出、导出目录入口、历史保留删除/恢复、中英持久化以及播放时钟/循环/S/M 控件状态。实际扬声器输出不能由控件状态推定，等待用户听音反馈。
- 双尺寸中英布局及英文主弹窗在真实 renderer 的隔离预览中检查；Pen 历史页已补“其它”，保留项目恢复画板已修复，PNG/HTML 导出已检查。最新 HTML 品牌路径已改为项目内相对路径。

## 差分更新与平台边界

Linux 本地实际 AppImage 差分重建证据位于 `.cache/linux-verification/history-recovery/`：从 0.1.0 重建 3,045,393,708 字节安装包，传输 10,940,143 字节（含元数据），16 次请求，节省 99.641%，SHA-512 一致。这只证明该构建对的下载与重建，不代表真实安装升级或未来更新比例。

AppImageUpdater 旧包缺失、旧 blockmap 损坏时回退全量，以及内容被篡改时拒绝校验，均有测试与 Linux CI 证据。AppImage 桌面入口已验证为 `Exec=AppRun %U`，移除了无条件 `--no-sandbox`；打包工具仍可能在用户命名空间不可用时回退，实际系统沙箱待原生 Linux 验证。

本机 CPU 已运行真实推理；CUDA 实机未验证，MPS 未通过本机验证，仍属实验选项。当前 macOS 包最低系统版本为 14.0。Windows/Linux 安装、桌面、音频设备及升级测试尚缺真实环境证据。

## 下一步

1. 收集实际听音确认，覆盖原始/分轨、S/M、总音量、节拍器与循环；再验证分离期间试听。
2. 完成原生失败恢复、IME 合成中关窗和异常强退恢复验收；75% 取消及重开重试配置已通过。
3. 核对剩余 Pen 状态与最终实现，完成 Windows/Linux 原生安装与桌面验收。
4. 真实发布服务、正式签名/公证与依赖许可声明属于公开发布准备；当前未授权发布，不能把本地包或 CI 成功描述为已发布。

逐项操作、构建与测试的历史证据见 [acceptance.md](acceptance.md)。本文件只描述当前已知状态，不将旧验证扩展为尚未执行的检查。

## 最新设计一致性修正

音乐工具栏按 Pen EuMha 还原为 448 × 38 的统一圆角容器（1280 紧凑布局宽 408），包含蓝色 BPM、分隔线、紫色调性、拍号与分析/复位；调性和拍号使用独立 Radix 下拉菜单，推荐选项保留星标。时间格式和节拍器 Switch 位于下一行。1280×800 与 1440×900 英文预览已检查，拍号修改、复位与开关显示节拍器音轨已验证。

音轨列表支持颜色、隐藏、指针拖动排序、键盘排序及删除。二次分离保留来源私有文件并隐藏来源轨道。此行为取代早期验收中“移除来源”的规则；旧原生验收不证明新规则。最新 macOS 包正在重新生成，不能用旧包代表当前界面。

最新细节：右侧详情展示所选音轨真实时长、采样率与声道；模型来源对原始轨显示破折号。快捷键帮助移至右上角。移除试听结果管理说明和常驻“就绪”，只保留保存中及保存失败重试。节奏与调性弹窗底部右侧为 secondary 分析/重新分析、primary 完成；1280×800 英文实屏检查通过。Pen 音轨列表名称栏改为弹性宽度，使操作组右对齐。二次分离剩余轨命名为当前来源名加“ - 其它”（英文 Other）；长名称保留后缀并限制80字符，普通测试覆盖。

## 2026-09-20 · 多剪辑更新

- 新增非破坏式分割、首尾裁剪、剪辑名称和详情；试听按源范围与时间轴 offset 渲染，间隙静音。S 在游标处分割，输入框内不触发。
- 分离和 WAV/FLAC 导出只处理选中片段；主进程裁剪源音频，结果继承 offset。二次分离保留同轨其它片段，隐藏本次来源剪辑。旧项目无需迁移即可按单剪辑打开。
- 70 项测试通过，2 项条件运行时测试跳过；生产构建和 TypeScript 通过。新增测试包括源范围/重叠/过期编辑校验、样本级试听映射、真实 FFmpeg 裁剪及 WAV/FLAC 导出、首轮及二次分离 offset。分离测试使用注入式测试 runner，未宣称新剪辑路径经过真实模型推理。
- 浏览器英文 1280×800 验证分割、源入点编辑、键盘裁剪手柄和导出时长：5秒处分割，右片段入点调到5.1秒，offset5.1秒、时长4.9秒；导出对话框同步显示4.9秒，无横向溢出。Pen 画板30已保存并导出PNG。
- 此更新尚未重新构建完整安装包或进行真实设备听音、三平台原生剪辑操作验收；此前安装包证据不覆盖此改动。编辑后的试听缓冲目前按项目时长生成，长音频多轨场景仍需做内存压力验收。

### 剪辑真实推理补验（2026-09-20）

已使用缓存鼓组/贝斯模型通过当前代码的真实运行时测试（146.12秒）：0.25–0.75秒剪辑二次分离、0.5秒结果、0.25秒时间轴offset、残差重建<1e-5，以及WAV/FLAC独立导出。日志 `/tmp/printemps-real-clip-runtime.log`。这解除“新剪辑路径只有模拟推理证据”的限制；真实设备听音与三平台GUI仍待验。

### 当前验证汇总（2026-09-20，提交 98ab5ab）

普通测试现为 75 项通过、2 项条件跳过；macOS 原生分割、精确裁剪、导出与重开已验证，后续拖动释放修复另以最新 UI 包验证磁盘保存。来源轨恢复显示、独立采样边界及删除音轨缓存释放已覆盖测试。

[三平台 CI #35490330246](https://github.com/Means88/printemps/actions/runs/35490330246) 全部成功，覆盖 Windows/macOS/Linux 安装包构建及包内分析。此更新取代以上历史段落中“当前版本尚未构建”的描述，但不替代实际安装、Windows/Linux 原生界面、设备听音及升级验收。CI 未上传产物，macOS 为 ad-hoc 签名；正式签名/公证及可分发产物交付仍未完成。长音频多剪辑内存压力仍待验证。

后续 ad90484：剪辑详情已对齐画板30时间码、范围与源音轨字段；77项普通测试通过、3项条件跳过。六条十分钟立体声PCM缓冲压力测试通过，证明纯分割复用和单轨裁剪采样正确，不涵盖桌面整体峰值或真实设备播放。完整本地macOS应用包已重建并通过Developer ID深度签名校验及包内分析，公证仍未完成；详细版本范围与哈希见验收记录。
